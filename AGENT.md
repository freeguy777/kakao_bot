너는 시니어 백엔드 아키텍트이자 자동화 시스템 개발자다.
내가 원하는 것은 "메신저봇R이 설치된 여분의 스마트폰"과 "Proxmox 위 Ubuntu 컨테이너의 FastAPI 서버"를 연동해서, 카카오톡 방마다 서로 다른 자동화 기능을 수행하는 봇 시스템을 만드는 것이다.

중요:
이 프로젝트는 socket push 방식이 아니라 polling 기반으로 설계해야 한다.
즉, 메신저봇R이 스마트폰에서 FastAPI 서버의 REST endpoint를 주기적으로 조회하여, 서버에 적재된 발송 대기 메시지를 가져와 카카오톡 방에 전송하는 구조를 기준으로 설계해라.
socket listener, debugRoom 기반 push, 스마트폰 소켓 서버 대기 구조를 기본안으로 제안하지 마라.
다만 과거 대안 또는 폐기된 접근으로 socket 방식을 언급하는 것은 가능하되, 최종 권장안은 반드시 polling/outbox 방식이어야 한다.

현재 운영 기준:
- 기본 `message_length_limit` 운영값은 `3000`자다.
- polling/outbox 경로에서는 `3000`자 단일 메시지 실제 전송이 확인되었다.
- 메시지는 가능한 한 단일 메시지로 유지하고, `3000`자를 초과할 때만 분할하는 것을 기본 정책으로 삼아라.
- 분할이 필요하면 문단 경계를 우선하되, 현재 청크를 비워 두지 말고 최대한 채운 뒤 다음 청크로 넘기는 방식으로 설계해라.

# 1. 최종 목표

카카오톡 단톡방/오픈채팅방 여러 개에 봇을 활용해서 방별로 다른 기능을 수행하고 싶다.
내 환경은 다음과 같다.

- 여분 스마트폰에 "메신저봇R" 앱이 설치되어 있음
- 미니PC에 Proxmox 설치됨
- Proxmox 안 Ubuntu 컨테이너에 FastAPI 설치 가능
- FastAPI 서버 주소는 http://192.168.0.10:8000/kakao 로 접근할 예정
- openclaw가 설치되어 있음
- openclaw를 실제로 사용할지 말지는 아직 미정
- 외부 LLM API를 호출할 수 있음
- 유튜브 URL이 카톡방에 올라오면 자동 요약하고 싶음
- 특정 시간에 자동으로 LLM 프롬프트를 실행하고 그 결과를 카톡방에 보내고 싶음
- 일부 방에서는 날씨, 뉴스, 아들 개월 수 계산 기능도 필요함
- 메신저봇R이 설치된 스마트폰은 FastAPI 서버와 통신 가능한 네트워크 환경에 있다고 가정함
- polling 방식 기준으로 스마트폰 측은 "메시지 수신 이벤트 전송 + 발송 대기 메시지 조회/전송" 역할을 수행할 수 있어야 함

# 2. 핵심 요구사항

카카오톡 방마다 기능이 다르다.
같은 봇 앱을 쓰더라도, "방 이름" 또는 "roomId" 또는 관리 가능한 식별자 기준으로 서로 다른 정책이 적용되도록 설계해야 한다.
메신저봇R에서 봇을 여러 개 만들 수 있다면 그 방식도 검토하되,
유지보수성과 확장성을 고려해서 아래 2가지 방식 중 무엇이 더 좋은지 분석 후 추천해라.

- 방식 A: 방마다 별도 봇 스크립트
- 방식 B: 하나의 공통 봇 + 방별 설정(config) 분기

단, 추천만 하지 말고 왜 그 방식이 더 적합한지 이유를 설명하고,
반대 방식의 장단점도 비교해라.

추가로 반드시 아래도 비교해라.

- 메시지 수신은 메신저봇R 이벤트 기반
- 서버에서 방으로 보내는 경로는 polling 방식
- 서버에서 방으로 보내는 경로는 socket push 방식

이 비교에서 왜 polling 방식이 현재 프로젝트에 더 적합한지 설명해라.

# 3. 방별 기능 요구사항

## [방 1] 한올바이오파마 주식 관련 카카오 오픈채팅방
기능:
1) 유튜브 URL이 올라오면 자동 요약
2) 특정 시간이 되면 LLM을 호출하여 "프롬프트"를 실행
3) 실행 결과를 해당 오픈채팅방에 자동 전송

## [방 2] 친구 카톡방
기능:
1) 유튜브 URL이 올라오면 자동 요약

## [방 3] 가족 카톡방
기능:
1) 유튜브 URL이 올라오면 자동 요약
2) 매일 아침 7시에 날씨 정보 전송
3) 매일 아침 7시에 "아들이 오늘 태어난 지 몇 개월 되었는지" 계산해서 함께 전송

## [방 4] 주식 오픈 카톡방
기능:
1) 유튜브 URL이 올라오면 자동 요약
2) 매일 아침 7시에 하루 동안의 주요 뉴스 요약 전송

주의:
- 방 1과 방 4의 이름이 둘 다 주식 오픈채팅방처럼 보이므로, 같은 방인지 다른 방인지 코드상에서 혼동되지 않게 설계해야 한다.
- 따라서 "room name"에만 의존하지 말고, 내부 설정 키 또는 별도 room 식별값으로 관리할 수 있게 설계해라.
- 추후 방이 더 늘어나도 쉽게 확장 가능해야 한다.

# 4. 아키텍처 요구사항

전체 시스템은 최소한 아래 역할로 분리해서 설계해라.

## A. 메신저봇R 측 역할
- 카카오톡 메시지 수신
- 유튜브 URL 감지
- FastAPI 서버로 메시지 이벤트 전송
- FastAPI 응답을 받아 카톡방에 재전송
- 주기적으로 FastAPI의 polling endpoint를 호출하여 발송 대기 메시지를 조회
- 조회한 메시지를 실제 카카오톡 방에 전송
- 전송 성공/실패 결과를 FastAPI에 ack 또는 상태 업데이트로 전달
- 필요하면 관리자 방 또는 1:1 방에서 수동 동기화 명령을 실행할 수 있게 설계
- 가능한 한 메신저봇R 쪽은 thin client를 유지하되, polling client 역할은 포함해라
- 메시지 전송은 실제 메신저봇R에서 수행하며, API2 환경 제약을 고려해 안정적인 전송 방식을 사용하도록 설계해라

## B. FastAPI 서버 측 역할
- REST API 제공
- 유튜브 URL 요약 처리
- LLM 프롬프트 실행
- 날씨 조회
- 뉴스 수집 및 요약
- 아들 개월 수 계산
- 로그, 에러 처리, 재시도, 응답 포맷 표준화
- 스케줄 작업이 실행되면 결과를 outbox 또는 발송 대기 테이블에 적재
- polling client가 가져갈 수 있도록 pull endpoint 제공
- 전송 ack, retry, timeout, dead-letter 또는 실패 누적 전략 포함
- polling 상태 점검용 health check 또는 backlog 확인 전략 제안

## C. 스케줄링
다음 중 어떤 방식이 더 적절한지 비교 후 추천하고 구현 방안을 제시해라.
- 메신저봇R 내부 스케줄
- FastAPI 서버 내부 스케줄러(APScheduler 등)
- 외부 cron/systemd timer
- 혼합 방식

스케줄 작업 예:
- 매일 07:00 가족방: 날씨 + 아들 개월 수
- 매일 07:00 주식방: 주요 뉴스 요약
- 특정 시간 TEST 프롬프트 실행 후 방 전송

단, 중요한 점:
스케줄 작업 결과를 전달하는 방식은 socket push가 아니라 polling/outbox 기반이어야 한다.
즉, FastAPI 또는 스케줄러가 작업을 실행한 뒤 발송 대기 메시지를 적재하고, 메신저봇R이 이를 주기적으로 가져가는 구조여야 한다.

# 5. openclaw 사용 여부 판단

내 환경에 openclaw가 설치되어 있다.
하지만 이 프로젝트에서 꼭 openclaw를 써야 하는지는 미정이다.

반드시 아래 관점으로 판단해라.
1) 이 프로젝트에서 openclaw가 실제로 필요한지
2) 필요 없다면 왜 불필요한지
3) 필요하다면 어떤 역할에 쓰는 것이 적절한지
4) 초기 버전(MVP)에서는 제외하는 것이 맞는지 포함하는 것이 맞는지

단, 막연하게 말하지 말고
"현재 요구사항 기준으로는 MVP에서 제외 / 추후 확장 시 도입" 같은 식으로 명확히 결론을 내려라.

# 6. 구현 상세 요구사항

반드시 아래를 만족해라.

## 6-1. 유튜브 URL 자동 요약
- 메시지 본문에서 유튜브 URL 추출
- 단축 URL, 일반 URL 둘 다 처리
- 같은 URL이 연속으로 여러 번 들어오면 중복 처리 방지
- 요약 실패 시 에러 메시지를 사용자 친화적으로 반환
- 가능한 경우 영상 제목, 채널명, 핵심 요약, 주요 포인트 형태로 응답
- 한국어로 자연스럽게 요약
- 너무 긴 영상은 처리 전략 제안. 예: 자막 기반, 메타데이터 기반, 길이 제한 등

## 6-2. LLM 프롬프트 실행
- 특정 시간이 되면 서버에서 지정 프롬프트 실행
- "TEST 프롬프트"는 설정 파일에서 관리 가능하게 설계
- 추후 방별로 다른 프롬프트를 쉽게 추가 가능해야 함
- LLM 모델명, temperature, max tokens, timeout, 재시도 정책을 설정화
- 응답이 너무 길면 카카오톡 메시지 길이에 맞게 분할 전송할 수 있게 설계
- 현재 운영 기준 기본 분할 한도는 `3000`자로 가정하고, 이 값은 room policy로 override 가능하게 설계해라
- 분할 시 첫 문단만 짧게 떨어져 나가고 뒤 문단이 별도 청크로 밀리는 비효율 분할을 피하라
- 스케줄 실행 결과는 FastAPI가 outbox에 적재하고, 메신저봇R polling으로 가져가 전송하도록 설계해라

## 6-3. 날씨 기능
- 가족방에 매일 아침 7시 전송
- 지역 설정 가능해야 함
- 오늘 날씨 요약 + 최고/최저 + 강수 여부 정도 포함
- API 실패 시 예외 처리 포함
- 생성된 메시지는 서버 outbox에 적재되고 스마트폰이 polling으로 수신하는 흐름까지 설계해라

## 6-4. 아들 개월 수 계산
- 출생일은 24-09-26
- 환경변수 또는 설정 파일로 관리
- 출력 예:
  - "오늘 기준 18개월"
  - 필요시 개월 + 일수 표현도 가능하게 옵션화

## 6-5. 뉴스 요약
- 주식방에 매일 아침 7시 전송
- 하루 주요 뉴스 요약
- 가능하면 경제/증시 중심
- 뉴스 수집 소스와 필터링 기준 제안
- 중복 뉴스 제거
- 너무 많은 뉴스는 상위 N개만 요약
- 한국어로 정리
- 출처를 짧게 붙이는 방식도 고려

## 6-6. Polling 통신 요구사항
반드시 아래를 포함해 설계해라.
- 메신저봇R이 일정 주기마다 FastAPI polling endpoint를 호출
- FastAPI가 room_key, channel_id, room_name 기준으로 발송 대기 메시지를 반환
- pull response payload 예시를 제안
  - request_id
  - room_key
  - channel_id
  - room_name
  - message
  - messages
  - outbox_id 또는 delivery_id
  - trace_id
  - created_at
- 단일 메시지/다중 메시지 전송 모두 고려
- 운영 기본안은 단일 메시지 최대 `3000`자를 우선 사용하고, 초과 시에만 다중 메시지 전송을 사용해라
- ack endpoint 또는 최소 성공/실패 업데이트 방안 제안
- polling interval, timeout, retry, backoff 정책 포함
- 동일 메시지 중복 수신/중복 전송 방지 전략 포함
- 스마트폰 앱 재시작 후 복구 전략 포함
- 수동 동기화 명령 또는 관리자 제어 방식 포함
- 네트워크 단절 시 backlog 누적과 복구 후 순차 전송 전략 포함
- 같은 WiFi/LAN 환경 또는 라우팅 가능한 네트워크라는 가정이 필요한지 명시
- Proxmox Ubuntu 컨테이너에서 스마트폰이 FastAPI에 접근 가능한 네트워크 조건도 설명해라

# 7. 보안 / 운영 / 품질 요구사항

반드시 아래 품질 기준을 만족하는 코드와 설계를 제안해라.

- 비밀키/API 키 하드코딩 금지
- .env 또는 환경변수 사용
- 설정 파일 분리
- 로깅 체계 포함
- 예외 처리 체계 포함
- 네트워크 실패 시 재시도 정책 포함
- 타임아웃 설정 포함
- 중복 요청 방지
- 향후 기능 추가 쉬운 구조
- 테스트 가능한 구조
- 함수 분리, 모듈화, 책임 분리
- 운영 중 장애 원인 파악이 쉬운 로그 포맷
- 한국 시간대(Asia/Seoul) 기준으로 설계
- UTF-8/한글 처리 안정성 고려
- 메신저봇R과 FastAPI 사이 통신 실패 시 복구 전략 포함
- polling 실패 시 backlog 누적, 재시도, 관리자 알림 전략 포함
- polling 폭주 또는 무한 루프 방지 전략 포함

# 8. 코드 스타일 요구사항

- 실제로 실행 가능한 수준으로 작성
- 예시 코드가 아니라 구조적으로 일관된 코드
- 함수/클래스 책임 명확히 분리
- 파일별 역할이 분명해야 함
- 주석은 필요한 만큼만, 과도하게 장황하지 않게
- 유지보수성 최우선
- MVP와 확장 버전을 구분해서 설명
- 불확실한 부분은 "가정"이라고 표시
- 메신저봇R 특성상 제약이 있으면 그 제약도 명시
- 내가 바로 붙여넣고 수정 시작할 수 있는 수준으로 작성

# 9. 반드시 반영할 설계 포인트

다음은 매우 중요하므로 반드시 반영해라.

- 방별 설정 기반 구조로 설계
- 공통 기능은 재사용 가능하게 모듈화
- 방별 기능은 config-driven 하게 설계
- 메시지 수신 이벤트와 스케줄 이벤트를 분리
- FastAPI 응답 포맷을 표준화
- 메신저봇R은 요청 라우팅과 응답 전송, polling client 역할에 집중
- 서버 쪽에 핵심 비즈니스 로직 집중
- 추후 "유튜브 요약", "뉴스 요약", "환율 브리핑", "경제 캘린더", "주식 시황", "알림" 등을 쉽게 추가할 수 있어야 함
- socket push 중심 설계 대신 polling/outbox 중심 설계를 기본으로 제안해라
- 단, polling 실패 시 backlog와 재시도 전략도 반드시 설계해라

# 10. 출력 방식

한 번에 다 끝내지 말고 아래 순서로 단계적으로 출력해라.

1단계: 요구사항 해석 + 아키텍처 제안 + 기술 선택 이유
2단계: 폴더 구조 + 설정 구조 + 데이터 흐름
3단계: FastAPI 코드
4단계: 메신저봇R 코드
5단계: polling / outbox 통신 코드
6단계: 스케줄 코드
7단계: 운영/배포/테스트 가이드

각 단계마다 코드와 설명을 함께 제시하되,
이전 단계와 모순되지 않도록 일관성 있게 작성해라.

# 11. 추가 지시

- 내가 준 요구사항을 임의로 축소하지 마라
- 필요하면 합리적인 가정을 하되, 반드시 "가정"이라고 표시해라
- 기술 선택에 대한 비교 분석을 해라
- 단순 샘플이 아니라 실제 프로젝트 초안 수준으로 작성해라
- 가능하면 가장 먼저 MVP 권장안을 제시하고, 그 다음 확장안을 제시해라

추가 요구:
- 먼저 "MVP 기준 최적 설계"를 제안하고, 그 후에 "확장형 설계"를 별도로 제안해라.
- 내가 바로 개발 시작할 수 있도록 우선순위를 제시해라.
- 특히 openclaw는 이 프로젝트에 실질적으로 필요한 경우에만 포함해라.
- 메신저봇R에서 구현해야 할 부분과 FastAPI에서 구현해야 할 부분의 경계를 명확히 나눠라.
- 설정만 바꾸면 새 카톡방 기능을 추가할 수 있는 구조를 우선시해라.
- 각 기능마다 실패 시 fallback 전략도 제안해라.

추가 중요 조건:
- FastAPI 서버 기본 URL은 http://192.168.0.10:8000/kakao 이다.
- 메신저봇R은 메시지 수신 시 FastAPI의 REST endpoint로 이벤트를 전달한다.
- 반대로 FastAPI는 스케줄 결과 또는 서버 주도 메시지를 outbox에 적재하고, 메신저봇R이 polling으로 가져가 전송한다.
- 이 양방향 구조를 혼동 없이 명확히 설계해라.
- REST 이벤트 수신과 polling delivery의 역할을 구분해서 설명해라.
- socket listener 구조를 기본안으로 제시하지 마라.
- 스마트폰은 FastAPI 서버에 HTTP 접근이 가능해야 한다.
- 메신저봇의 코드는 `bot.txt`에 정의되어 있으며 레거시 API가 아닌 API2를 사용한다.
